//Scripts do AppTodo_Lau
/*var lista = document.querySelector('.App #loop ul');
var inputElement = document.querySelector('.App input');

var todos = [
    'Angola',
    'Brasil',
    'Viana'
];

function renderTodo() {
    for(lenda of todos) {
        var topico = document.createElement('li');
        var topicoText = document.createTextNode(lenda);

        topico.appendChild(topicoText);
        lista.appendChild(topico);
    }
}

renderTodo(); 
*/